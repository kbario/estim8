import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header class="navbar bg-base-100 px-4">
      <div class="text-2xl text-primary">estim8</div>
    </header>
  );
});
